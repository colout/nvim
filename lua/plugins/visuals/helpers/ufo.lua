return {
  { -- better looking folding, but it defaults to closed and isn't too well documented, and folding is annoying anyway (can't search folds)
    --    'kevinhwang91/nvim-ufo',
    --    dependencies = { 'kevinhwang91/promise-async' },
    --    opts = {},
  },
}
